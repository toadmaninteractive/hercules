<UserControl x:Class="Hercules.Shell.View.GeneralSettingsTabView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:shell="clr-namespace:Hercules.Shell"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance shell:GeneralSettingsTab}"
             d:DesignHeight="300" d:DesignWidth="500">
    <StackPanel HorizontalAlignment="Left">
        <TextBlock Margin="0,0,0,10" Text="General Settings" FontWeight="Black" />
        <CheckBox Content="Load last database on startup" IsChecked="{Binding Settings.LoadLastDatabaseOnStartup.Value}" />
    </StackPanel>
</UserControl>
