<UserControl x:Class="Hercules.AI.View.AiChatToolView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Hercules.AI.View" xmlns:ai="clr-namespace:Hercules.AI" xmlns:ae="http://icsharpcode.net/sharpdevelop/avalonedit" xmlns:i="http://schemas.microsoft.com/xaml/behaviors" xmlns:hc="clr-namespace:Hercules.Controls" d:DataContext="{d:DesignInstance Type=ai:AiChatTool}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <DockPanel LastChildFill="True">
        <StackPanel Orientation="Vertical" DockPanel.Dock="Bottom" Margin="4">
            <TextBox Text="{Binding UserPrompt, UpdateSourceTrigger=PropertyChanged}" Height="70" TextWrapping="Wrap" />
            <Button HorizontalAlignment="Right" Command="{Binding SubmitCommand}" Margin="4" Height="20" Width="160"  Content="Submit" />
        </StackPanel>
        <Grid Background="#ffe9ecfa" Margin="4">
            <Border
                        BorderThickness="1"
                        BorderBrush="Gray">
                <ae:TextEditor
                            Name="TextEditor"
                            FontFamily="Courier New"
                            IsReadOnly="True"
                            Document="{Binding ChatLog}"
                            ShowLineNumbers="False"
                            VerticalScrollBarVisibility="Visible">
                    <ae:TextEditor.Options>
                        <ae:TextEditorOptions EnableImeSupport="False" HighlightCurrentLine="True" ConvertTabsToSpaces="True" />
                    </ae:TextEditor.Options>
                    <ae:TextEditor.ContextMenu>
                        <ContextMenu>
                            <MenuItem Command="{x:Static ApplicationCommands.Copy}" />
                            <MenuItem Command="{x:Static ApplicationCommands.SelectAll}" />
                        </ContextMenu>
                    </ae:TextEditor.ContextMenu>
                </ae:TextEditor>
            </Border>
        </Grid>
    </DockPanel>
</UserControl>
