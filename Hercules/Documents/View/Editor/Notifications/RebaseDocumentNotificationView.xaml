<UserControl x:Class="Hercules.Documents.View.Editor.RebaseDocumentNotificationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:editor="clr-namespace:Hercules.Documents.Editor"
             mc:Ignorable="d"
             Height="30"
             d:DataContext="{d:DesignInstance editor:RebaseDocumentNotification}"
             d:DesignHeight="30" d:DesignWidth="900">
    <Border HorizontalAlignment="Stretch" BorderThickness="1" BorderBrush="Gainsboro" Background="Beige">
        <DockPanel>
            <TextBlock Margin="8,0,8,0" Text="{Binding Text}" VerticalAlignment="Center" />
            <Button
                Content="Rebase"
                Width="80"
                HorizontalAlignment="Left"
                Margin="5,3,5,3"
                Command="{Binding RebaseCommand}" />
        </DockPanel>
    </Border>
</UserControl>
